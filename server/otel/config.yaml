# OpenTelemetry Collector (contrib) — OTLP, trace’y: Jaeger + Tempo (OTLP) + logi: debug; metryki: otel:8889
# Klienci: 4317 gRPC, 4318 HTTP. Prometheus w Compose skrapuje: otel:8889
receivers:
  otlp:
    protocols:
      grpc:
        endpoint: 0.0.0.0:4317
      http:
        endpoint: 0.0.0.0:4318

processors:
  batch: {}

exporters:
  debug:
    verbosity: detailed
  # Jaeger all-in-one: odbiornik OTLP gRPC (sieć Docker: host jaeger, port 4317)
  otlp/jaeger:
    endpoint: jaeger:4317
    tls:
      insecure: true
  # Grafana „Drilldown / Traces” wymaga Tempo — Jaeger zostaje do UI http://localhost:16686
  otlp/tempo:
    endpoint: tempo:4317
    tls:
      insecure: true
  prometheus:
    endpoint: 0.0.0.0:8889

service:
  pipelines:
    traces:
      receivers: [otlp]
      processors: [batch]
      exporters: [debug, otlp/jaeger, otlp/tempo]
    metrics:
      receivers: [otlp]
      processors: [batch]
      exporters: [prometheus]
    logs:
      receivers: [otlp]
      processors: [batch]
      exporters: [debug]
